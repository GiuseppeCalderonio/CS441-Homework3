medium {

    datacenters = [

        {
            # type of the datacenter, it can be simple, medium or large
            type = "medium"
            # number of datacenters of this type
            count = 1
        }
    ]

    cloudlets = [
        {
            # type of the cloudlet, it can be light, easy, medium or large
            type = "medium"
            # number of instances of this cloudlet
            count = 3
        }
    ]
    vms = [
        {
            # type of VM, it can be simple, medium or large [String]
            type = "medium"
            # number of VMs of this type to instntiate [Long]
            count = 3
        }
    ]
}