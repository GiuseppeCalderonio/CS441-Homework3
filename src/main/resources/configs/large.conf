large {

    datacenters = [

        {
            # type of the datacenter, it can be simple, medium or hard
            type = "large"
            # number of datacenters of this type
            count = 1
        }
    ]

    cloudlets = [
        {
            # type of the cloudlet, it can be light, easy, medium or hard
            type = "large"
            # number of instances of this cloudlet
            count = 3
        }
    ]
    vms = [
        {
            # type of VM, it can be simple, medoum or large [String]
            type = "large"
            # number of VMs of this type to instntiate [Long]
            count = 3
        }
    ]
}